import React from 'react';
import { render } from '@testing-library/react';
import App from './App';


beforeEach(() => {

});

afterEach(() => {
});

test('renders learn react link', () => {
  render(<App />);
});